"use client";

// The original /journey remains the fixed Tokyo Marathon+ proof of concept.
// This route deliberately reuses the renderer only for generated trips.
export { default } from "../page";
